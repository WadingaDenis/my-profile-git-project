version: 1
frontend:
  phases:
    # No build phase needed for static HTML
    build:
      commands:
        - echo "Building static site..."
  artifacts:
    baseDirectory: ./
    files:
      - index.html
      - about.html
      - skills.html
      - projects.html
      - experience.html
      - contact.html
      - style.css
      - README.md
  cache:
    paths: []
