version: 1
frontend:
  phases:
    # No build phase needed for static HTML
    build:
      commands:
        - echo "Building static site..."
  artifacts:
    baseDirectory: ./
    files:
      - index.html
      - den-profile.html
      - README.md
  cache:
    paths: []
